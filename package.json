{
  "name": "channel-os",
  "version": "0.4.0",
  "description": "Telegram Channel OS v0.4 Analytics Pro",
  "main": "src/app.js",
  "scripts": {
    "postinstall": "prisma generate",
    "start": "prisma migrate deploy && node src/app.js",
    "dev": "nodemon src/app.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "dotenv": "^16.4.7",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "prisma": "^6.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
